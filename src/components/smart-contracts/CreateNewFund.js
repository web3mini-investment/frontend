const createNewFund = ({underlyingAsset, offerClosingTime, orderExpiration, maturity}) => {
    // smart contractの処理書く
    // const fs = require('fs');
    // const builtContract = JSON.parse(fs.readFileSync('./contracts/CollectiveInvestmentScheme', 'utf8'));
    // console.log(JSON.stringify(contract.abi));
    return {}
}

export default createNewFund
